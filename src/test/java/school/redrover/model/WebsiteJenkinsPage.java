package school.redrover.model;

import org.openqa.selenium.WebDriver;
import school.redrover.model.base.BasePage;

public class WebsiteJenkinsPage extends BasePage {

    public WebsiteJenkinsPage(WebDriver driver) {
        super(driver);
    }
}
